<div class='py-9'>
    <h1 class="text-center my-5">Yum-Yum-Burgers!!!</h1>
</div>

<div class='row align-items-center justify-content-center'>
    <form class="create-form" width=352>
        <div class="form-group" style="width: 352px;">
            <div class="form-group">
                <select class="form-control combo" id="cNew" name="customer_name" placeholder="Customer's name" class='rounded'>
                    {{#each customer}} {{> customers/customer customer_name=customer_name id=id}} {{/each}}
                </select>
                <select class="form-control combo" id="eNew" name="email" placeholder="Customer's email" class='rounded'>
                    {{#each customer}} {{> customers/customer customer_name=customer_name id=id}} {{/each}}
                </select>
                <select class="form-control combo" id="bNew" name="burger_name" placeholder="Burger name" class='rounded'>
                    {{#each burgers}} {{> burgers/burger-devoured this burger_name=burger_name id=id}} {{/each}}
                </select>
                <label for="qNew"></label>
                <input type="text" id="qNew" name="quantity" placeholder='Enter quantity of burger' class='rounded'>
            </div>

        <button type="submit" class="btn btn-success submitButton" href="#modalPopUp" rel="modal:open">Get Burger</button>
    </form>
</div> 